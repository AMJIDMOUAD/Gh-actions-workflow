name : cron-workflow
on:
  schedule:
    - cron: "2 * * * *"
  
jobs:
  cron-workflow:
    runs-on: ubuntu-latest
    steps:
     - name: Test cron
       run : echo " Hello Every 2 min is runing !!"
        
